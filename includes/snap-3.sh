#!/usr/bin/env bash

sudo snap install clion --classic --channel=stable
sudo snap install pycharm-professional --classic --channel=stable
sudo snap install intellij-idea-ultimate --classic --channel=stable
sudo snap install webstorm --classic --channel=stable
sudo snap install datagrip --classic --channel=stable
